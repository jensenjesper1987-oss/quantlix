# Base template for model containers
# Customize per model: COPY model/, requirements.txt
FROM pytorch/pytorch:2.1.0-cuda12.1-cudnn8-runtime

WORKDIR /app

# Model-specific: copy model files and requirements
# COPY model/ ./model/
# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt

# Inference server entrypoint
# CMD ["python", "serve.py"]

# Placeholder for scaffolding
CMD ["python", "-c", "print('Model container ready')"]
