# Dev-only: placeholder secret so deployment works locally.
# Prod: create secret manually (see docs/GO_LIVE.md)
apiVersion: v1
kind: Secret
metadata:
  name: api
  namespace: quantlix
type: Opaque
stringData:
  jwt-secret: dev-secret-change-me
  email-enabled: "true"
  sweego-api-key: ""
  sweego-auth-type: "api_token"
  smtp-user: ""
  smtp-password: ""
